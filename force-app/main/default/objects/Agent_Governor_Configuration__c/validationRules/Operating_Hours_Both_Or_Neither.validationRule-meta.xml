<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Operating_Hours_Both_Or_Neither</fullName>
    <active>true</active>
    <description>Operating hours must be set as a pair. Blocks a half-filled window (only Start or only End), which would otherwise be treated silently as always open. Leave both blank to run at all hours, or set both to define a window.</description>
    <errorConditionFormula>ISBLANK(Start_Hour__c) &lt;&gt; ISBLANK(End_Hour__c)</errorConditionFormula>
    <errorDisplayField>End_Hour__c</errorDisplayField>
    <errorMessage>Set BOTH Start Hour and End Hour to define operating hours, or leave BOTH blank to run at all hours. One without the other has no effect.</errorMessage>
</ValidationRule>
